我将根据您的要求优化日记生成、替换素材并合并重复打卡物品。

### 1. 提升日记生成的颗粒度
- **目标**：让生成的日记内容更加细腻、具体，富有画面感。
- **实施方案**：
  - 修改 `generateDiaryWithAIGC` 函数，在提示词中加入 **时间段（早晨/午后/夜晚）** 和 **感官细节（视觉、听觉、嗅觉）** 的要求。
  - 要求 AI 描述具体的 **微小观察**（例如：“屋檐下睡觉的小猫”、“刚出炉包子的热气”），而不仅仅是宏大的景点描述。

### 2. 替换素材库素材
- **目标**：使用 `场景` 文件夹中的素材替换现有图片。
- **实施方案**：
  - **房间背景**：将代码中硬编码的背景图 `img_v3_...` 替换为 `场景/场景.png`。
  - **橱柜弹窗背景**：修改 CSS `.cabinet-modal` 样式，使用 `场景/物件UI背景.png` 作为背景图。

### 3. 重复打卡物品合并显示（橱柜）
- **目标**：如果多次打卡同一地点，将纪念品合并展示并显示数量。
- **实施方案**：
  - 修改 `addCabinetItem` 逻辑：检查 `appState.cabinetItems` 中是否已存在相同地点的物品。
    - 如果存在，增加该物品的 `count` 计数。
    - 如果不存在，新增物品并初始化 `count: 1`。
  - 修改 `renderCabinetGrid` 渲染逻辑：如果物品数量大于 1，在图标右上角显示数量角标（如 "x2"）。

我将直接修改 `prototype.html` 文件来实现这些功能。
